name: Landingzone

on:
  push:
    branches-ignore:
      - main
    paths:
        - 'Landingzone/**'
        - '.github/workflows/template-Landingzone.yml'
        - '.github/workflows/main-Landingzone.yml'

  pull_request_target:
    types:
      - closed
    paths:
      - 'Idenitity/**'
  
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  test:
    if: github.event_name == 'workflow_dispatch' || 
        github.ref != 'refs/heads/main'
    uses: ./.github/workflows/template-Landingzone.yml
    with:
      environment: test
    secrets: inherit

  prod:
    if: (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
         github.event.pull_request.merged == true
    uses: ./.github/workflows/template-Landingzone.yml
    with:
      environment: prod
    secrets: inherit